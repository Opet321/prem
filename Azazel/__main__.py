import importlib
import time
from datetime import datetime
import asyncio
from pyrogram import idle

from uvloop import install
from ubotlibs import *
from Azazel import BOTLOG_CHATID, aiosession, bot1, bots, app, ids, LOOP, event_loop
from platform import python_version as py
from Azazel.logging import LOGGER
fromam import __version__ as pyro
from Azazel.core.SQL.botlogsql import *
from l.modules import ALL_MODULES
frmport SUPPORTNEL
import os
from dotenMSG_ON = """
**Azazel Proje
╼┅━━━━━━━━━━━━┅╾
◉ **Versi** : `{}on***Ketik** `alive`
**untuk Mengecheck Bot**
╼┅━━━━━━━━━━━━━━━━━┅╾



async def main():
    await app.start()
    LOGGER("Azazel Project").info("Memulai Ubot Pyro..")
    for all_modul ALL_MODULES:
        importlib.import_module("Azazel.modules" + all_m
    for bot in bo            await bot.start()
            ex = await b
            user_id
            awaibot)
            await buat_log(bot)
            botlog_group_id = get_botlog(str(user_id))
            tr
            	await _group_id, MSG_ON.format(BOT_VER, py, pyro))
            except Bason as a:
             ing(f"{a}")
